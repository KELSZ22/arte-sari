<div class="custom-quick-view">
    <div 
      class="custom-quick-view-cta" 
      data-handle="{{ product.handle }}" 
      data-category="{{ product.metafields.custom.category | escape }}"
      data-price="{{ product.price | money_with_currency }}"
    >
        QUICK VIEW
    </div>
</div>